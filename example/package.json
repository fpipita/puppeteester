{
  "name": "my-app",
  "version": "1.0.0",
  "description": "Puppeteester example usage",
  "main": "src/sum.js",
  "author": "Francesco Pipita <francesco.pipita@gmail.com>",
  "license": "GPL-3.0",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1"
  },
  "dependencies": {
    "lodash-es": "^4.17.15"
  },
  "scripts": {
    "test": "../src/cli/cli.js --sources ./src --coverage ci && echo 'tests passed' || echo 'tests failed'",
    "watch": "../src/cli/cli.js --sources ./src --coverage watch"
  }
}
