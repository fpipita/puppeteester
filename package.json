{
  "name": "@fpipita/puppeteester",
  "type": "module",
  "version": "0.0.1",
  "description": "Run client side tests within a headless Chrome instance.",
  "main": "index.js",
  "author": "Francesco Pipita <francesco.pipita@gmail.com>",
  "license": "GPL-3.0",
  "dependencies": {
    "@fpipita/esm-middleware": "^1.2.3",
    "chokidar": "^3.2.2",
    "express": "^4.17.1",
    "glob": "^7.1.5",
    "mocha": "^6.2.2",
    "puppeteer-core": "^2.0.0"
  },
  "devDependencies": {
    "commitizen": "^4.0.3",
    "conventional-changelog-cli": "^2.0.25",
    "cz-conventional-changelog": "^3.0.2",
    "eslint": "^6.6.0",
    "eslint-config-prettier": "^6.4.0",
    "eslint-plugin-prettier": "^3.1.1",
    "esm": "^3.2.25",
    "husky": ">=1",
    "lint-staged": "^9.4.2",
    "nodemon": "^1.19.4",
    "prettier": "^1.18.2"
  },
  "scripts": {
    "start": "node --inspect=0.0.0.0 --experimental-modules ./src/index.js",
    "start:dev": "nodemon --inspect=0.0.0.0 --experimental-modules ./src/index.js",
    "test": "mocha --ui tdd --require esm",
    "commit": "git-cz",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
    "preversion": "yarn test && yarn eslint .",
    "version": "yarn changelog",
    "postversion": "git push && git push --tags",
    "release": "yarn version && yarn publish --access=public"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
